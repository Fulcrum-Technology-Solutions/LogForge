#!/usr/bin/env python3
"""
LogForge - Synthetic event log generator command line interface
"""

import sys
import os

# Add the parent directory to the path so we can import logforge
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from logforge.cli import main

if __name__ == '__main__':
    main()