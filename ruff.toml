line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "W"]
ignore = ["E203"]  # compatible with black slices

[lint.isort]
known-first-party = ["logforge"]
combine-as-imports = true
